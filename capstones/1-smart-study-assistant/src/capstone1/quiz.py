"""Capstone 1 -- quiz generation.

Part of the starter scaffold in Chapter 17. `app.py` calls
`quiz.generate(topic, n, store)`. Not printed in the book; implement
MCQ generation over the indexed material here.
"""


def generate(topic: str, n: int = 5, store: str = ".store"):
    # TODO: retrieve context on `topic` from the vector store and
    # generate `n` multiple-choice questions with answers.
    raise NotImplementedError("Fill this in -- see functional requirement 3.")
