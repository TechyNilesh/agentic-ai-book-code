"""Capstone 1 -- retrieval + citation.

Part of the starter scaffold in Chapter 17. `app.py` calls
`retriever.answer(question, store)`. Not printed in the book;
implement retrieval-augmented answering with page-number citations
here (see Chapter 7, "Retrieval-Augmented Generation").
"""


def answer(question: str, store: str = ".store") -> str:
    # TODO: retrieve top-k chunks from the vector store at `store`
    # and answer `question`, citing page numbers.
    raise NotImplementedError("Fill this in -- see Chapter 7.")
